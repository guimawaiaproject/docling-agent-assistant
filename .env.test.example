# Docling Agent v3 — Variables pour les tests
# Copier vers .env.test et adapter les valeurs

TEST_BASE_URL=http://localhost:8000
TEST_FRONTEND_URL=http://localhost:5173

# DB de test (Neon branch ou PostgreSQL local)
# Utiliser une base dédiée pour ne pas polluer la prod
TEST_DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Optionnel : Storj pour test_storage (sinon skip)
# STORJ_BUCKET=docling-factures-test
# STORJ_ACCESS_KEY=
# STORJ_SECRET_KEY=
# STORJ_ENDPOINT=https://gateway.storjshare.io

# Optionnel : tests extraction réelle (consomme des tokens Gemini)
# GEMINI_API_KEY=...
