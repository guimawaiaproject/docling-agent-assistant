# Docling Agent v3 — Staging Environment
# ============================================================
# This file is committed to the repo as a TEMPLATE.
# Actual secrets must be injected via CI/CD or Render env vars.

# --- Database (Neon staging branch) ---
DATABASE_URL=postgresql://docling_staging:PASSWORD@ep-xxx-staging-pooler.eu-central-1.aws.neon.tech/docling_staging?sslmode=require

# --- Google Gemini ---
GEMINI_API_KEY=your-staging-gemini-key

# --- Auth ---
JWT_SECRET=change-this-staging-secret-32chars

# --- Model ---
DEFAULT_AI_MODEL=gemini-3-flash-preview

# --- CORS (staging PWA URL) ---
PWA_URL=https://staging.docling-agent.netlify.app

# --- Storj (staging bucket) ---
STORJ_BUCKET=docling-factures-staging
STORJ_ACCESS_KEY=
STORJ_SECRET_KEY=
STORJ_ENDPOINT=https://gateway.storjshare.io

# --- Monitoring ---
SENTRY_DSN=
ENVIRONMENT=staging

# --- Watchdog (disabled in staging) ---
WATCHDOG_ENABLED=false
