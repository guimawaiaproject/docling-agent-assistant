# Backlog — Idées et idées brutes

Dépose ici les idées de features, améliorations ou bugs à traiter.

**Format suggéré** : un fichier `.md` par idée (ex: `export-pdf-multi-factures.md`).

Quand une idée est prête à être planifiée → déplacer le fichier dans `1_planning/`.
