# Exemple — Idée de feature

**Idée** : Export PDF multi-factures (plusieurs factures en un seul PDF).

**Contexte** : Les utilisateurs demandent souvent à exporter plusieurs factures traitées en un seul document.

**Priorité** : À définir.

---

Quand cette idée est prête à être planifiée → déplacer dans `1_planning/export-pdf-multi/` et créer le dossier avec README, spec, design.
