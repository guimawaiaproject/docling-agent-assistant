# docker-compose.yml — Docling Agent Premium
version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    command: [ "streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0" ]
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://api:8000
    depends_on:
      api:
        condition: service_healthy
    volumes:
      - ./app.py:/app/app.py

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - WATCHDOG_FOLDER=/app/Docling_Factures
    volumes:
      - docling_data:/app/data # Persist SQLite locally
      - ./backend:/app/backend # Hot reload in dev
      - ./Docling_Factures:/app/Docling_Factures # Dossier Magique Local <-> Container
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')" ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  docling_data:
