# Dev dependencies — install: pip install -r requirements-dev.txt
# Préférer: cd apps/api && uv sync (inclut dev-deps)
-r requirements.txt

# Tests
pytest>=8.4
pytest-asyncio>=1.2
httpx>=0.27
faker>=30

# Pre-commit
pre-commit>=4.0
