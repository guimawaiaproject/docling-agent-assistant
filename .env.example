# Docling Agent v2 — Environment
# Required:
GEMINI_API_KEY=YOUR_API_KEY_HERE
DATABASE_URL=postgresql://user:password@serveur.neon.tech/dbname?sslmode=require

# Optional — OCR.space (reduces Gemini token usage by 90%):
# OCR_SPACE_API_KEY=your_ocr_space_key

# Optional — "Dossier Magique" (Auto-sync des factures déposées, type OneDrive)
# WATCHDOG_FOLDER=Docling_Factures

# Optional — Google Cloud integration:
# GOOGLE_CREDENTIALS_PATH=credentials.json
# GOOGLE_DRIVE_FOLDER_ID=your_drive_folder_id
# GOOGLE_SHEET_ID=your_sheet_id

# Optional — API Security (comma-separated API keys):
# API_KEYS=key1,key2,key3

# Optional — Monitoring:
# SENTRY_DSN=https://your-sentry-dsn

# Optional — CORS origins (comma-separated):
# CORS_ORIGINS=https://your-app.com,https://localhost:3000
