# Docling Agent v3 - Environment
# ============================================================

# --- OBLIGATOIRE ---
GEMINI_API_KEY=YOUR_GEMINI_API_KEY
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# --- Modele IA (optionnel, defaut: gemini-3-flash-preview) ---
# DEFAULT_AI_MODEL=gemini-3-flash-preview

# --- Dossier Magique / Watchdog (optionnel) ---
# WATCHDOG_FOLDER=./Docling_Factures
# WATCHDOG_ENABLED=true

# --- Stockage Cloud S3-compatible (optionnel, pour archivage PDF) ---
# STORJ_BUCKET=docling-factures
# STORJ_ACCESS_KEY=
# STORJ_SECRET_KEY=
# STORJ_ENDPOINT=https://gateway.storjshare.io

# --- CORS / PWA URL en production (optionnel) ---
# PWA_URL=https://docling-agent.netlify.app

# --- Authentification JWT (OBLIGATOIRE) ---
# OBLIGATOIRE — générer avec : openssl rand -hex 32
JWT_SECRET=change-this-to-a-long-random-string
# JWT_EXPIRY_HOURS=24

# --- Auth feature flag (optionnel) ---
# VITE_AUTH_REQUIRED=false   # Frontend : false = pas de redirection login
# FREE_ACCESS_MODE=true       # Backend : accès provisoire sans auth (guest partagé)

# --- TVA / IVA frontend (optionnel, defaut: 0.21 = 21% IVA espagnol) ---
# VITE_TVA_RATE=0.21

# --- Monitoring Sentry (optionnel, fortement recommandé en prod) ---
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# ENVIRONMENT=production
